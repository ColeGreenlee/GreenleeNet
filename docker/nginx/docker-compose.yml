version: "3"
services:
  nginx:
      image: nginx:alpine
      restart: always
      labels:
        - "traefik.enable=true"
        - 'traefik.frontend.rule=Host:www.greenlee.io'
        - "traefik.port=80"
      volumes:
        - /var/www/:/usr/share/nginx/html